export type tipCard = {
    image: string;
    title: string;
    description: string;
};

export type limitationCard = {
    image: string;
    title: string;
    description: string;
};

export type radioProps = {
    id: string;
    value: string;
    label: string;
    checked?: boolean;
};